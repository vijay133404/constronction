<%= form_with(model: inquiry_type, local: true) do |form| %>
  <% if inquiry_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(inquiry_type.errors.count, "error") %> prohibited this inquiry_type from being saved:</h2>

      <ul>
      <% inquiry_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :inquiry_type_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
